# MATLAB data files - static, not tracked in git
*.mat
# CSV files - static, not tracked in git
*.csv
# papers - just for claude to learn
*.pdf
# python bytecode cache
__pycache__/
# batch ACG exports - ~250 generated PNGs per session, too many to track
acg_export/
# Office temp lock files (e.g. ~$Prototype.pptx)
~$*
